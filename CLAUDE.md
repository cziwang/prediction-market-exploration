# prediction-market-exploration

All v1 code (batch fetchers, live streaming, MM strategy, notebooks, tests) has been moved to `v1/`.

See `v1/CLAUDE.md` for the full v1 project context.

## Environment

- Python 3.12, virtualenv at `.venv/`
- AWS credentials via `~/.aws/credentials` or EC2 IAM role
- `.env` has `KALSHI_API_KEY_ID` and `KALSHI_PRIVATE_KEY_PATH`
